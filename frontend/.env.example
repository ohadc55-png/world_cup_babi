# URL של ה-backend. בפיתוח לוקאלי: http://localhost:8000
# בייצור (Railway): URL של שירות ה-backend
VITE_API_BASE_URL=http://localhost:8000

# מפתח VAPID ציבורי לאופציית push (מאותו keypair שב-backend/.env)
VITE_VAPID_PUBLIC_KEY=
